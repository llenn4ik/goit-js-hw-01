"use strickt";


let input;
let total = 0;

while (true) {

    let typeNumber = prompt('Введите число, ');
    typeNumber = Number(typeNumber);
    if (!typeNumber) break;
    total = 0 + $ {
        typeNumber
    };

}
alert(`Общая сумма:  ${total}`);






let input;
let total = 0;


let typeNumber = prompt(`ВВедите число: `)
typeNumber = Number(typeNumber);

let rezult = `total +${typeNumber} `;

while (expression) {

    if (typeNumber === Nan) {
        message = `Попробуйте еще раз!`;
    } else {
        rezult = `total +${typeNumber} `
    }
}

При загрузке страницы пользователю предлагается в prompt ввести
число.Ввод добавляется к значению переменной total.
Операция ввода числа продолжается до тех пор, пока пользователь
не нажмет кнопку Cancel в prompt.
После того как пользователь прекратил ввод нажав кнопку Cancel,
показать alert со строкой 'Общая сумма чисел равна [сумма]'.