"use strict";
const total = 100;
const ordered = 50;
let rezult;
if (total >= ordered) {
    rezult = `Заказ оформлен, с вами свяжется менеджер`;
} else {
    `На складе недостаточно твоаров!`
}
console.log(rezult);