"use strickt";


let input;
let total = 0;

while (true) {

    let typeNumber = prompt('Введите число, ');
    typeNumber = Number(typeNumber);
    if (!typeNumber) break;
    total = 0 + $ {
        typeNumber
    };

}
alert(`Общая сумма:  ${total}`);