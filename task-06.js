"use strickt";


let typeNumber;
let total = 0;

while (true) {

    let typeNumber = prompt('Введите число, ');
    typeNumberCifer = Number(typeNumber);
    if (!typeNumber) break;
    total = 0 + $ {
        typeNumberCifer
    };

}
alert(`Общая сумма:  ${total}`);